<div class="container">
<h1> Write a new Review </h1>

<%= simple_form_for([@booking, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit %>
  <% end %>

<%= link_to "Back to Memories", my_memories_path %>

</div>